UNIX:

If you haven't done so already, extract the archive contents to a temporary location

Open setup.py and change the following to match your setup: 

gdal_lib='/usr/lib '
gdal_inc='/usr/include/gdal'
gdal_vers='1.7.0'

WINDOWS:
?

